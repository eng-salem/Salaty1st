<Window x:Class="QuoteNotificationWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Islamic Quote"
    Height="250" Width="450"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    Topmost="True"
    ShowInTaskbar="False"
    ResizeMode="NoResize"
    WindowStartupLocation="Manual">

    <Window.Resources>
        <FontFamily x:Key="CairoFont">pack://application:,,,/PrayerWidget;component/Resources/cairo.ttf#Cairo</FontFamily>
    </Window.Resources>

    <Border x:Name="QuoteBorder" Background="#E61A1A2E" CornerRadius="15" BorderBrush="#9C27B0" BorderThickness="2" Margin="10" Opacity="0.95">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.6" Direction="270"/>
        </Border.Effect>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header with Copy Button -->
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="💬" FontSize="18" Margin="0,0,8,0"/>
                    <TextBlock Text="Islamic Quote" FontSize="14" FontWeight="SemiBold" Foreground="#9C27B0"/>
                </StackPanel>
                <Button x:Name="BtnCopy"
                       Grid.Column="2"
                       Content="📋 Copy"
                       FontSize="12"
                       FontWeight="SemiBold"
                       Foreground="#9C27B0"
                       Background="Transparent"
                       BorderBrush="#9C27B0"
                       BorderThickness="1"
                       Padding="8,4"
                       Cursor="Hand"
                       ToolTip="Copy quote to clipboard"
                       Click="BtnCopy_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="6"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#9C27B0"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <!-- Quote Text with ScrollViewer -->
            <Border Grid.Row="1" Background="#1A1A2E" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              FlowDirection="RightToLeft">
                    <TextBlock x:Name="TxtQuote"
                              Text="Loading quote..."
                              FontSize="16"
                              Foreground="White"
                              TextWrapping="Wrap"
                              FontStyle="Italic"
                              FlowDirection="RightToLeft"
                              FontFamily="{StaticResource CairoFont}"/>
                </ScrollViewer>
            </Border>

            <!-- Source -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock x:Name="TxtSource"
                          Text="- Source"
                          FontSize="12"
                          Foreground="#9C27B0"
                          FontStyle="Italic"
                          FlowDirection="RightToLeft"/>
                <Button x:Name="BtnClose"
                       Content="✕"
                       FontSize="12"
                       FontWeight="Bold"
                       Foreground="#888"
                       Background="Transparent"
                       BorderThickness="0"
                       Width="24"
                       Height="24"
                       Margin="15,0,0,0"
                       Cursor="Hand"
                       Click="BtnClose_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="12"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF4444"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
