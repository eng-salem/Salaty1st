--- MainWindow.xaml.vb.orig
+++ MainWindow.xaml.vb
@@ -954,28 +954,25 @@
     End Function
 
     Private Sub ShowFirstRunMessage()
-        Dim title = LocalizationService.GetString("first_run_title", "First Run Setup")
-        Dim message = LocalizationService.GetString("first_run_message", "Welcome to Salaty First! 🕌" & vbCrLf & vbCrLf &
-                       "To get accurate prayer times for your location:" & vbCrLf &
-                       "1. Click 'OK' to open Settings" & vbCrLf &
-                       "2. Click '📍 Use IP Location' or enter your city" & vbCrLf & vbCrLf &
-                       "You can also adjust appearance and notification settings.")
-        Dim result As MessageBoxResult = MessageBox.Show(message, title,
-                       MessageBoxButton.OK,
-                       MessageBoxImage.Information)
-
-        ' Open settings directly after user clicks OK
-        If result = MessageBoxResult.OK Then
-            ShowSettingsWindow()
+        ' First, show language selection dialog
+        Dim languageDialog As New LanguageSelectionWindow()
+        languageDialog.Owner = Me
+        
+        If languageDialog.ShowDialog() = True Then
+            ' Language selected, reload localization
+            InitializeLocalization()
+            ApplyTranslations()
         End If
+        
+        ' Now open settings with first run mode (will auto-detect location)
+        ShowSettingsWindow(firstRun:=True)
     End Sub
 
-    Private Sub ShowSettingsWindow()
-        Dim settingsWindow As New SettingsWindow(_settingsManager, isFirstRun:=False)
+    Private Sub ShowSettingsWindow(Optional firstRun As Boolean = False)
+        Dim settingsWindow As New SettingsWindow(_settingsManager, isFirstRun:=firstRun)
         settingsWindow.Owner = Me
